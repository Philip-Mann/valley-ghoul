import './info.css';
import { useState } from 'react';

const Info = ({ id, isClicked, products }) => {

    return (
        <div className="info-container">
        </div>
    )
}

export default Info;

